/** Один клик: свой запрос к RAG + инструкция к LLM (тот же пайплайн, что у обычного чата). */

export type PromptTemplateId = "investor" | "student" | "slide";

export const PROMPT_TEMPLATES: Record<
  PromptTemplateId,
  {
    /** Текст в пузырьке пользователя */
    label: string;
    /** Запрос к RAG для подбора чанков */
    ragQuery: string;
    /** Пользовательское сообщение к LLM (задача стиля) */
    llmInstruction: string;
  }
> = {
  investor: {
    label: "Для инвестора",
    ragQuery:
      "ключевые показатели цифры рынок риски инвестиции выводы эффективность перспективы окупаемость метрики",
    llmInstruction:
      "Сформируй короткий меморандум для инвестора: суть возможности или продукта, рынок, риски, деньги и выводы. Только факты из контекста; если данных мало — укажи, чего не хватает. Без markdown.",
  },
  student: {
    label: "Для студента",
    ragQuery: "основные понятия содержание определения разделы выводы ключевые идеи структура",
    llmInstruction:
      "Объясни содержание материала простым языком, как для студента: основные понятия, логика текста, что важно запомнить. Только по контексту. Без markdown.",
  },
  slide: {
    label: "Тезисы на 1 слайд",
    ragQuery: "ключевые положения выводы тезисы суть заголовок итоги кратко главное",
    llmInstruction:
      "Собери материал для одного слайда: одна строка — заголовок темы, затем 3–5 коротких тезисов (по одной мысли в строке, можно нумерация). Только из контекста. Без markdown.",
  },
};
